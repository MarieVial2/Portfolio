{% extends 'base.html.twig' %}

{% block title %}Realisation{% endblock %}

{% block body %}
    <h1>Realisation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ realisation.id }}</td>
            </tr>
            <tr>
                <th>TitreRealisation</th>
                <td>{{ realisation.titreRealisation }}</td>
            </tr>
            <tr>
                <th>DescriptionRealisation</th>
                <td>{{ realisation.descriptionRealisation }}</td>
            </tr>
            <tr>
                <th>LienRealisation</th>
                <td>{{ realisation.lienRealisation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_realisation_index') }}">back to list</a>

    <a href="{{ path('app_realisation_edit', {'id': realisation.id}) }}">edit</a>

    {{ include('realisation/_delete_form.html.twig') }}
{% endblock %}
